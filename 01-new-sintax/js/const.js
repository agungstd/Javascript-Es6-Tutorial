// Define a constant for the value of PI
const PI = 3.14;

// Define a constant for the API URL
const API_URL = "https://jsonplaceholder.typicode.com";

// Define a user object with name and age properties
const user = { name: "Pojok Code", age: 30 };

// Update the age property of the user object
user.age = 31;
console.log(user); // Output the updated user object

// Define an array of colors
const colors = ["red", "green", "blue"];

// Add a new color to the array
colors.push("yellow");
console.log(colors); // Output the updated array of colors
